defmodule BeamwareWeb.DeviceChannelTest do
  use BeamwareWeb.ChannelCase

  # direct channel tests need to be reconsidered

  # alias BeamwareWeb.DeviceChannel

  # setup do
  #   {:ok, _, socket} =
  #     socket("user_id", %{some: :assign}, websocket: true)
  #     |> subscribe_and_join(DeviceChannel, "device:lobby")

  #   {:ok, socket: socket}
  # end

end
