<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Beamware</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          <%= if Map.get(@conn.assigns, :user) do %>
            <ul class="nav nav-pills pull-right">
              <li><a href="<%= session_path(@conn, :delete) %>">Log Out</a></li>
            </ul>
          <% end %>
        </nav>
        <a href="<%= session_path(@conn, :new) %>">
          <span class="logo"></span>
        </a>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <%= if Map.get(@conn.assigns, :user) do %>
	<ul class="nav nav-tabs">
          <%= for {name, path} <- navigation_links(@conn) do %>
            <li role="presentation" class="<%= if @conn.request_path == path, do: "active", else: "" %>">
              <a href="<%= path %>"><%= name %></a>
            </li>
          <% end %>
	</ul>
      <% end %>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
