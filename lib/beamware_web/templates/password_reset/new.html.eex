<%= form_for @changeset, password_reset_path(@conn, :create), fn f -> %>
  <p>
    <label>
      Email:
      <%= text_input f, :email %>
      <%= error_tag f, :email %>
    </label>
  </p>

  <%= submit "Submit" %>
<% end %>
