<p>Hi <%= @user.name %>,</p>

<p>We received a request to reset your password on NervesHubWWW. Click on the link below to begin:</p>

<a href="<%= password_reset_path(NervesHubWWWWeb.Endpoint, :new_password_form, @user.password_reset_token) %>">Reset Password</a>

<p>The link is only valid for a short time.</p>
